// Codigo jQuery

$(document).ready(function(){  //Esto sirve para cargar los elementos 
    $("#vg").hide(3000);
})

// Todo inicia con un "$" y consiguiente se agrega los demas requerimientos, en este caso en el index se agrega un "id" de identificador a una etiqueta y se hace llamar acá
// Dentro  de la función, para exactamente llamar a uno se da con "#" y el id, luego de eso se denota la función o acción que esperas que haga, en este caso se utilizó la función
// ".hide()" Lo que hace es ocultar la etiqueta, estan puesto los numeros para reafirmar cuando en cuanto tiempo se ejecutará.